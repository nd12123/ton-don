// components/header/index.tsx  (SERVER component)
/*
import { cookies } from "next/headers";
import HeaderClient from "./HeaderClient";

export default async function Header() {
  const cookieStore = await cookies();        // Next 15 — async
  const isAdminFromCookie = cookieStore.get("admin")?.value === "1";
  //return <HeaderClient isAdminFromCookie={isAdminFromCookie} />;
}*/
